const grid = document.querySelector(".grid");
const startButton = document.getElementById("start");
const score = document.getElementById("score");

function createGrid() {
  // create 100 of these elemenrs
  for (let i = 0; i < 100; i++) {
    const square = document.createElement("div");
     //create element

     
    console.log(square);

  }
}
createGrid();
